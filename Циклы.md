**Вопрос 1**

Сколько раз выполнится следующий цикл и почему:

```cs
int i = 5;
while(i > 0)
{
    i *= 3;
    i *= -1;
}
```

**Ответ:** Цикл выполнится 1 раз.

Причина: На первой итерации цикла значение переменной i сначала умножается на 3 (i =15), а затем умножается на -1 (i = -15). При следующей проверке условия while(i > 0) оно оказывается ложным, так как -15 > 0 — ложь. Цикл немедленно прекращает выполнение.

**Вопрос 2**

Дан следующий цикл:

```cs
int j = 2;

for (int i = 1; i < 100; i = i + 2)

{

    j = j - 1;

    while(j < 15)

    {

        j = j + 5;

    }

}
```

**Сколько раз в этом цикле будет выполняться строка j = j - 1;**

**Ответ:** 50 раз Объяснение: Строка j = j - 1; находится во внешнем цикле for. Этот цикл управляется переменной i.

- Цикл for начинается с i = 1.
- Условие выполнения: i < 100.
- Шаг изменения: i = i + 2.

Цикл `for` последовательно перебирает нечетные числа: 1 ... 99. Количество итераций внешнего цикла равно 50. Так как строка j = j - 1; выполняется ровно один раз за каждую итерацию внешнего цикла, она выполнится 50 раз.

**Вопрос 3**

Что будет выведено на консоль в результате выполнения следующего цикла:

```cs
for(int i = 1; i < 3; i++)

{

    switch (i)

    {

        default:

            Console.WriteLine($"i = {i++}");

            break;
      }

}
```

**Варианты ответов:**

- Программа не скомпилируется
- Ничего не будет выведено на консоль
- Консоль будет иметь вывод i = 1
- Консоль будет иметь вывод i = 1 i = 2

**Ответ:** Консоль выведет i=1 Объяснение:

1. На первой итерации `i=1`. Внутри default происходит вывод `i=1`, а затем пост-инкремент i++ меняет значение i на 2.
2. `break` выходит из `switch` и из тела цикла `for`.
3. В заголовке цикла `for` происходит еще один инкремент `i++`, меняя значение `i` на 3.
4. Условие цикла `i < 3` становится ложным (`3 < 3`), и цикл завершается.

**Упражнение 1**

За каждый месяц банк начисляет к сумме вклада 7% от суммы. Напишите консольную программу, в которую пользователь вводит сумму вклада и количество месяцев. А банк вычисляет конечную сумму вклада с учетом начисления процентов за каждый месяц. Для вычисления суммы с учетом процентов используйте цикл for. Для ввода суммы вклада используйте выражение Convert.ToDecimal(Console.ReadLine()) (сумма вклада будет представлять тип decimal).

```cs
using System;

class Program
{
    static void Main()
    {
        Console.WriteLine("Введите начальную сумму вклада:");
        decimal deposit = Convert.ToDecimal(Console.ReadLine());

        Console.WriteLine("Введите количество месяцев, на которое открыт вклад:");
        int months = Convert.ToInt32(Console.ReadLine());

        decimal monthlyRate = 0.07m; // суффикс 'm' указывает, что это decimal
        
		// стандартное форматирование F2 (Fixed point, 2 знака после запятой)
        Console.WriteLine($"\nНачальная сумма: {deposit:F2}");
        Console.WriteLine($"Срок вклада: {months} месяцев");

        for (int i = 1; i <= months; i++)
        {
            decimal interestGained = deposit * monthlyRate;
            deposit = deposit + interestGained;

            Console.WriteLine($"Месяц {i}: Начислено {interestGained:F2}. Сумма стала: {deposit:F2}");
        }

        Console.WriteLine($"\nСумма вклада через {months} месяцев составит: {deposit:F2}");
    }
}
```

**Упражнение 2**

```cs
using System;

class Program
{
    static void Main()
    {
        Console.WriteLine("Введите начальную сумму вклада:");
        decimal deposit = Convert.ToDecimal(Console.ReadLine());

        Console.WriteLine("Введите количество месяцев, на которое открыт вклад:");
        int Months = Convert.ToInt32(Console.ReadLine());

        decimal monthlyRate = 0.07m;

        // стандартное форматирование F2 (Fixed point, 2 знака после запятой)
        Console.WriteLine($"\nНачальная сумма: {deposit:F2} ₽");
        Console.WriteLine($"Срок вклада: {Months} месяцев");

        int currentMonth = 1;

        while (currentMonth <= Months)
        {
            decimal interestGained = deposit * monthlyRate;
            deposit += interestGained;

            Console.WriteLine($"Месяц {currentMonth}: Начислено {interestGained:F2} ₽. Сумма стала: {deposit:F2} ₽");

            currentMonth++;
        }

        Console.WriteLine($"\nСумма вклада через {Months} месяцев составит: {deposit:F2} ₽");
    }
}
```

**Упражнение 3**

```cs
using System;

class Program
{
    static void Main()
    {
        for (int i = 1; i <= 10; i++)
        {
            for (int j = 1; j <= 10; j++)
            {
                Console.Write($"{i * j}\t"); // табуляция для выравнивания
            }
            Console.WriteLine();
        }
    }
}
```

**Упражнение 4**

```cs
using System;

class Program
{
    static void Main()
    {
        double num1, num2;

        Console.WriteLine("Умножение двух чисел");

        // бесконечный цикл для повторного запроса ввода
        while (true)
        {
            Console.WriteLine("\nВведите первое число:");
            if (!double.TryParse(Console.ReadLine(), out num1))
            {
                Console.WriteLine("Некорректный ввод первого числа.");
                continue; // возврат к началу
            }

            Console.WriteLine("Введите второе число:");
            if (!double.TryParse(Console.ReadLine(), out num2))
            {
                Console.WriteLine("Некорректный ввод второго числа.");
                continue; // возврат к началу
            }

            // проверка диапазона
            if (num1 >= 0 && num1 <= 10 && num2 >= 0 && num2 <= 10)
            {
                // если числа допустимы, выходим из бесконечного цикла
                break;
            }
            else
            {
                // если хотя бы одно число недопустимо
                Console.WriteLine("Введенные числа недопустимы.");
            }
        }

        // выполнение только после выхода из цикла
        double result = num1 * num2;
        Console.WriteLine($"Результат умножения: {num1} * {num2} = {result}");
    }
}
```

**Практическая**

```cs
using System;
using System.Threading;

class Program
{
    static void Main()
    {
        // Аналоги предметов:
        // hasOrb -> Энергоядро (для дешифровки)
        // hasSword -> Ионный резак (для борьбы с дроном/системами)
        // hasRuneKey -> Мастер-пароль (для скрытых терминалов)
        bool hasOrb = false;
        bool hasSword = false;
        bool hasRuneKey = false;

        bool gameIsRunning = true;
        string currentState = "Step1";

        Console.WriteLine("TERMINAL: ОБЪЕКТ «ГЕЛИОС-7»");
        Console.WriteLine("Вы на орбите заброшенной станции. Ваша цель — главный блок данных.");

        while (gameIsRunning)
        {
            switch (currentState)
            {
                case "Step1":
                    Console.WriteLine("\nВнешний шлюз");
                    Console.WriteLine("Вы у входа. Датчики показывают отсутствие кислорода за дверью.");
                    Console.WriteLine("1. Начать декомпрессию и войти.");
                    Console.WriteLine("2. Прервать миссию и вернуться на шаттл.");

                    string choice1 = Console.ReadLine();
                    switch (choice1)
                    {
                        case "1": currentState = "Step2"; break;
                        case "2": currentState = "Ending_Coward"; break;
                        default: Console.WriteLine("Ошибка ввода."); break;
                    }
                    break;

                case "Step2":
                    Console.WriteLine("\nРеакторный отсек");
                    Console.WriteLine("В центре тускло мигает резервный генератор.");
                    if (hasOrb) Console.WriteLine("Разъем генератора пуст.");
                    Console.WriteLine("1. Извлечь Энергоядро.");
                    Console.WriteLine("2. Идти в Технический тоннель (Сектор А).");
                    Console.WriteLine("3. Идти в Жилой блок (Сектор Б).");

                    string choice2 = Console.ReadLine();
                    switch (choice2)
                    {
                        case "1": currentState = "Step3"; break;
                        case "2": currentState = "Step4"; break;
                        case "3": currentState = "Step5"; break;
                        default: Console.WriteLine("Ошибка ввода."); break;
                    }
                    break;

                case "Step3":
                    Console.WriteLine("\nГенераторная");
                    if (hasOrb) Console.WriteLine("Ядро уже у вас.");
                    else
                    {
                        Console.WriteLine("Вы извлекаете ядро. Основное освещение гаснет, но у вас есть источник питания.");
                        hasOrb = true;
                        Thread.Sleep(1500);
                    }
                    currentState = "Step2";
                    break;

                case "Step4":
                    Console.WriteLine("\nСектор А (Технический)");
                    Console.WriteLine("На мониторе мигает надпись: 'Протокол возврата активен'.");
                    Console.WriteLine("1. Двигаться дальше к блоку фильтрации.");
                    Console.WriteLine("2. Вернуться в реакторный отсек.");

                    string choice4 = Console.ReadLine();
                    switch (choice4)
                    {
                        case "1": currentState = "Step6"; break;
                        case "2": currentState = "Step2"; break;
                        default: Console.WriteLine("Ошибка ввода."); break;
                    }
                    break;

                case "Step5":
                    Console.WriteLine("\nСектор Б (Жилой)");
                    Console.WriteLine("Коридор поврежден. Впереди: яркая вспышка оголенных проводов или шум выходящего газа.");
                    Console.WriteLine("1. Пройти через провода.");
                    Console.WriteLine("2. Идти на звук газа.");

                    string choice5 = Console.ReadLine();
                    switch (choice5)
                    {
                        case "1": currentState = "Step20"; break; // Короткое замыкание (пауки)
                        case "2": currentState = "Step21"; break; // Путь к озеру
                        default: Console.WriteLine("Ошибка ввода."); break;
                    }
                    break;

                case "Step6":
                    Console.WriteLine("\nБлок фильтрации воды");
                    Console.WriteLine("Огромный резервуар разбит. В жидкости что-то блестит.");
                    Console.WriteLine("1. Обыскать разбитый резервуар.");
                    Console.WriteLine("2. Проверить терминал слева.");
                    Console.WriteLine("3. Проплыть через затопленный коридор.");

                    string choice6 = Console.ReadLine();
                    switch (choice6)
                    {
                        case "1": currentState = "Step7"; break; // Ионный резак
                        case "2": currentState = "Step18"; break; // Мастер-пароль
                        case "3": currentState = "Step8"; break;
                        default: Console.WriteLine("Ошибка ввода."); break;
                    }
                    break;

                case "Step7":
                    Console.WriteLine("\nРезервуар");
                    if (hasSword) Console.WriteLine("Вы здесь уже всё забрали.");
                    else
                    {
                        Console.WriteLine("Среди обломков вы находите Ионный Резак!");
                        hasSword = true;
                    }
                    currentState = "Step6";
                    break;

                case "Step8":
                    Console.WriteLine("\nВнутренняя палуба");
                    Console.WriteLine("Вы перебрались через затопленный отсек. Впереди две консоли: синяя и стальная.");
                    Console.WriteLine("1. Использовать синюю консоль.");
                    Console.WriteLine("2. Использовать стальную консоль.");

                    string choice8 = Console.ReadLine();
                    switch (choice8)
                    {
                        case "1": currentState = "Step9"; break;
                        case "2": currentState = "Step10"; break;
                        default: Console.WriteLine("Ошибка ввода."); break;
                    }
                    break;

                case "Step9":
                    Console.WriteLine("\nСиняя консоль");
                    if (hasRuneKey)
                    {
                        Console.WriteLine("Система требует Мастер-пароль. Авторизация прошла успешно!");
                        Console.WriteLine("Открывается секретный сервисный лаз.");
                        currentState = "Step19";
                    }
                    else
                    {
                        Console.WriteLine("Консоль заблокирована. Вы пытаетесь взломать её, но активируется сброс атмосферы!");
                        currentState = "Ending_Void";
                    }
                    break;

                case "Step10":
                    Console.WriteLine("\nОтсек охраны");
                    Console.WriteLine("Здесь находится боевой дрон в режиме гибернации.");
                    Console.WriteLine("1. Попробовать тихо прокрасться мимо сенсоров.");
                    Console.WriteLine("2. Попытаться активировать дрон для помощи.");
                    Console.WriteLine("3. Атаковать дрон, пока он отключен.");

                    string choice10 = Console.ReadLine();
                    switch (choice10)
                    {
                        case "1": currentState = "Step11"; break;
                        case "2": currentState = "Step12"; break;
                        case "3": currentState = "Step16"; break;
                        default: Console.WriteLine("Ошибка ввода."); break;
                    }
                    break;

                case "Step11":
                    Console.WriteLine("\nОбход завершен");
                    Console.WriteLine("Вы проскользнули мимо дрона. Впереди дверь в серверную.");
                    currentState = "Step13";
                    break;

                case "Step12":
                    Console.WriteLine("\nАктивация дрона");
                    Console.WriteLine("Дрон включается, его линза загорается красным.");
                    Console.WriteLine("1. Попробовать передать код дружбы.");
                    Console.WriteLine("2. Вступить в открытый бой.");

                    string choice12 = Console.ReadLine();
                    switch (choice12)
                    {
                        case "1": currentState = "Step17"; break;
                        case "2":
                            currentState = hasSword ? "Ending_BraveDeath" : "Ending_Captured";
                            break;
                        default: Console.WriteLine("Ошибка ввода."); break;
                    }
                    break;

                case "Step13":
                    Console.WriteLine("\nСерверное ядро");
                    Console.WriteLine("Вы у цели. Блок данных защищен мощным силовым полем.");
                    currentState = "Step14";
                    break;

                case "Step14":
                    Console.WriteLine("\nСиловое поле");
                    Console.WriteLine("Для отключения защиты требуется огромный скачок напряжения.");
                    Thread.Sleep(1000);
                    if (hasOrb)
                    {
                        Console.WriteLine("Вы подключаете Энергоядро! Защита выгорает от перегрузки.");
                        currentState = "Step15";
                    }
                    else
                    {
                        Console.WriteLine("У вас нет источника энергии. Станция блокирует отсек. Вы заперты.");
                        currentState = "Ending_Trapped";
                    }
                    break;

                case "Step15":
                    Console.WriteLine("\nДанные получены");
                    Console.WriteLine("Вы копируете файлы. Станция начинает вибрировать — запущен протокол очистки!");
                    Console.WriteLine("1. Использовать энергию Ядра для ускорения шаттла.");
                    Console.WriteLine("2. Бросить всё и бежать в спасательную капсулу.");

                    string choice15 = Console.ReadLine();
                    switch (choice15)
                    {
                        case "1":
                            currentState = hasOrb ? "Ending_Victory" : "Ending_Corrupted";
                            break;
                        case "2": currentState = "Ending_EmptyHanded"; break;
                        default: Console.WriteLine("Ошибка ввода."); break;
                    }
                    break;

                case "Step16":
                    Console.WriteLine("\nДиверсия");
                    if (hasSword)
                    {
                        Console.WriteLine("С помощью Ионного Резака вы перерезаете кабели питания дрона. Путь свободен.");
                        currentState = "Step11";
                    }
                    else
                    {
                        Console.WriteLine("Вы бьете по дрону арматурой. Это пробуждает его защиту, и он хватает вас.");
                        currentState = "Ending_Captured";
                    }
                    break;

                case "Step17":
                    Console.WriteLine("\nПереговоры с ИИ");
                    Console.WriteLine("Дрон запрашивает: 'Предъявите пропуск или оплатите доступ ресурсами'.");
                    if (hasOrb)
                    {
                        Console.WriteLine("1. Отдать Энергоядро дрону.");
                        Console.WriteLine("2. Отказаться.");
                        string choice17 = Console.ReadLine();
                        if (choice17 == "1")
                        {
                            Console.WriteLine("Дрон забирает ядро и отключает турели.");
                            hasOrb = false;
                            currentState = "Step11";
                        }
                        else currentState = "Ending_Captured";
                    }
                    else currentState = "Ending_Captured";
                    break;

                case "Step18":
                    Console.WriteLine("\nТерминал управления");
                    if (hasRuneKey) Console.WriteLine("Вы уже скачали пароли.");
                    else
                    {
                        Console.WriteLine("Вы извлекаете из памяти терминала Мастер-пароль!");
                        hasRuneKey = true;
                    }
                    currentState = "Step6";
                    break;

                case "Step19":
                    Console.WriteLine("\nСервисный тоннель");
                    Console.WriteLine("Вы вышли прямо к блоку данных в обход всех защит!");
                    Console.WriteLine("Но как только вы коснулись сервера, сработала механическая ловушка.");
                    currentState = "Ending_Greed";
                    break;

                case "Step20":
                    Console.WriteLine("\nКороткое замыкание");
                    Console.WriteLine("Из стен вылетают защитные нано-боты и атакуют вас!");
                    if (hasSword)
                    {
                        Console.WriteLine("Ионный резак создает защитное поле, и вы прорываетесь дальше.");
                        currentState = "Step6";
                    }
                    else currentState = "Ending_Spiders";
                    break;

                case "Step21":
                    Console.WriteLine("\nОбходной путь");
                    Console.WriteLine("Вы идете на звук газа и находите безопасный технический лаз к фильтрам.");
                    currentState = "Step6";
                    break;

                // КОНЦОВКИ
                case "Ending_Coward":
                    Console.WriteLine("\nОТКАЗ: Вы вернулись на шаттл. Репутация потеряна, станция осталась загадкой.");
                    gameIsRunning = false; break;

                case "Ending_Void":
                    Console.WriteLine("\nКРИТИЧЕСКАЯ ОШИБКА: Разгерметизация выбросила вас в открытый космос.");
                    gameIsRunning = false; break;

                case "Ending_Captured":
                    Console.WriteLine("\nОБЪЕКТ НЕЙТРАЛИЗОВАН: Вы заперты в стазис-поле охранной системы.");
                    gameIsRunning = false; break;

                case "Ending_Trapped":
                    Console.WriteLine("\nИЗОЛЯЦИЯ: Питание скафандра кончилось. Вы стали частью истории «Гелиоса».");
                    gameIsRunning = false; break;

                case "Ending_Victory":
                    Console.WriteLine("\nПОБЕДА: Вы вернулись с данными. Корпорация платит щедро. Вы — легенда.");
                    gameIsRunning = false; break;

                case "Ending_BraveDeath":
                    Console.WriteLine("\nГЕРОЙСКАЯ СМЕРТЬ: Вы уничтожили дрон, но получили смертельные ранения.");
                    gameIsRunning = false; break;

                case "Ending_Greed":
                    Console.WriteLine("\nЖАДНОСТЬ: Легкий путь привел в ловушку для воров. Вы заперты с данными навсегда.");
                    gameIsRunning = false; break;

                case "Ending_Spiders":
                    Console.WriteLine("\nДЕМОНТАЖ: Нано-боты разобрали ваш скафандр на атомы.");
                    gameIsRunning = false; break;

                case "Ending_Corrupted":
                    Console.WriteLine("\nСБОЙ: Без ядра данные повредили ваш нейроинтерфейс. Вы потеряли рассудок.");
                    gameIsRunning = false; break;

                case "Ending_EmptyHanded":
                    Console.WriteLine("\nВЫЖИВШИЙ: Вы спаслись, но станция взорвалась вместе с данными.");
                    gameIsRunning = false; break;
            }
        }
        Console.WriteLine("\nСВЯЗЬ ПРЕРВАНА.");
    }
}

```